# Cross-compilation configuration for ARM64 (Raspberry Pi 4)
# https://github.com/cross-rs/cross

[build.env]
passthrough = [
    "RUSTFLAGS",
]

[target.aarch64-unknown-linux-gnu]
# Use the default cross image which includes libpq for PostgreSQL
# The image provides: gcc-aarch64-linux-gnu, g++-aarch64-linux-gnu, and common libs
pre-build = [
    "dpkg --add-architecture arm64",
    "apt-get update && apt-get install -y libpq-dev:arm64 libssl-dev:arm64",
]
